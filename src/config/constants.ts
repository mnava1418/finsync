import { ButtonType } from "./types"

export const PATHS = {
    home: '/',
    login: '/login',
    signup: '/signup'
}

export const BTN_TYPES: ButtonType = {
    primary: 'fs-btn fs-btn-primary'
}

